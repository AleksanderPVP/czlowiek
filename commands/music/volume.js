module.exports = {
    name: `volume`,
    code: `$title[Głośność]
    $color[#9542f5]
    $description[:white_check_mark: Głośność ustawiona na: $message%]
    $footer[Polecenie wydane przez: $username]
    $volume[$message]
    `
}
